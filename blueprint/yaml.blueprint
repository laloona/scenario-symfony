# config/packages/scenario.yaml
when@dev:
    scenario_symfony:
        enabled: true
        allowed_envs: ['dev', 'test']

when@test:
    scenario_symfony:
      enabled: true
      allowed_envs: ['dev', 'test']
